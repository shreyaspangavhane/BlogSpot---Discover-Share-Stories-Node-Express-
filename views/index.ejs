<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, ;
            background-color: #f4f4f9;
            color: #333;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        
        h1 {
            color: #444;
            margin-bottom: 40px;
            font-size: 2.5rem;
        }
        
        .post {
            background-color: #fff;
            border: 1px solid #e1e1e1;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-bottom: 30px;
            width: 60%;
            max-width: 700px;
            transition: transform 0.2s ease-in-out;
        }
        
        .post:hover {
            transform: translateY(-5px);
        }
        
        .post h2 {
            font-style: italic;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 10px;
        }
        
        .post h4 {
            line-height: 1.6;
            margin-bottom: 20px;
        }
        
        .button-group {
            display: flex;
            gap: 15px;
        }
        
        a,
        button {
            text-decoration: none;
            font-size: 1rem;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
            display: inline-block;
            text-align: center;
        }
        
        a {
            color: white;
        }
        
        .btn-primary {
            background-color: #3498db;
            border: none;
        }
        
        .btn-primary:hover {
            background-color: #2980b9;
        }
        
        .btn-edit {
            background-color: #e67e22;
            border: none;
        }
        
        .btn-edit:hover {
            background-color: #d35400;
        }
        
        .btn-delete {
            background-color: #e74c3c;
            border: none;
            color: white;
        }
        
        .btn-delete:hover {
            background-color: #c0392b;
        }
        
        .btn {
            background-color: #27ae60;
            color: white;
            border: none;
            margin-top: 15px;
        }
        
        .btn:hover {
            background-color: #1e8449;
        }
        
        form {
            display: inline;
        }
        
        .img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
            margin-top: 15px;
            object-fit: cover;
            width: 100%;
            height: 300px;
            margin-bottom: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body>
    <h1>BlogSpot - Discover & Share Stories</h1>
    <% for (post of posts) { %>
        <div class="post">
            <h2>@
                <%= post.username %>
            </h2>
            <h4>
                <%= post.content %>
            </h4>
            <img src="<%= post.img %>
            " alt="image" class="img">
            <div class="button-group">
                <a href="http://localhost:3000/posts/<%= post.id %>" class="btn-primary">See in Detail</a>
                <a href="http://localhost:3000/posts/<%= post.id %>/edit" class="btn-edit">Edit Post</a>
                <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                    <button type="submit" class="btn-delete">Delete Post</button>
                </form>
            </div>
        </div>
        <% } %>

            <button class="btn">
        <a href="http://localhost:3000/posts/new">Create New Post</a>
    </button>
</body>

</html>